export const metadata = {
  title: "Poslovi",
  description:
    "Objavite posao ili pronađite dostupne poslove za majstore u Hrvatskoj."
};

export default function JobsLayout({ children }) {
  return children;
}
